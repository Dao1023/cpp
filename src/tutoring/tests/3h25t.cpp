#include <iostream>
#include <queue>
#include <ctime>

using namespace std;

const int t = 30;
const int ts = 25;

int run(queue<time_t> &q) {
    timeNow = time(nullptr);

    while (!q.empty)
}

int main() {
    queue<time_t> q;

    char input;
    while (1) {
        cin >> input;
        run(q);
    }
}