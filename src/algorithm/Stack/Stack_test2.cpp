#define _CRT_SECURE_NO_WARNINGS
#define MAXSIZE 5
#include <stdio.h>

int push(int *a, int top, int elem) {
    a[++top] = elem;
    return top;
}

int pop(int *a, int top) {
    if (top == -1) {
        
    }
}

int main() {
    int x;
}