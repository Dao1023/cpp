#include <bits/stdc++.h>

using namespace std;

// Basic definitions

typedef int ElemType;
typedef int Status;
typedef struct LNode{
    ElemType data;
    struct LNode* next;
}LNode, *LinkList;

// the main program

